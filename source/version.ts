export const MAJOR_VERSION = 1;
export const MINOR_VERSION = 0;
export const PATCH_VERSION = 0;
export const PREQL_VERSION = `${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}`;
export default PREQL_VERSION;
