interfaces:
  person:
    firstname:
      type: "VARCHAR"
      length: 64
      default: "Bob"
      comment: "Should not be abbreviated"
      nullable: True
    lastname:
      type: "VARCHAR"
      length: 64
      default: "McGobb"
# users:
# roles:
schema:
  testeroni:
    # enums:
    tables:
      people:
        comment: "A table of people"
        columns:
          firstname:
            type: "VARCHAR"
            length: 64
            default: "Bob"
            comment: "Should not be abbreviated"
            nullable: True
          lastname:
            type: "VARCHAR"
            length: 64
            default: "McGobb"
          age:
            type: "uint"
            length: 8
            default: 18
          ip:
            type: "inet"
        indexes:
          blorpy:
            keys:
              - column: "lastname"
            type: "primary"
    # views:
    # functions: