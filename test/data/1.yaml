interfaces:
  person:
    firstname:
      type: "varchar"
      length: 64
      default: "Bob"
      comment: "Should not be abbreviated"
      nullable: True
    lastname:
      type: "varchar"
      length: 64
      default: "McGobb"
# users:
# roles:
schema:
  testeroni:
    # enums:
    tables:
      people:
        comment: "A table of people"
        columns:
          firstname:
            type: "varchar"
            length: 64
            default: "Bob"
            comment: "Should not be abbreviated"
            nullable: True
          # lastname:
          #   type: "VARCHAR"
          #   length: 64
          #   default: "McGobb"
          age:
            type: "uint"
            length: 8
            default: 18
          ip:
            type: "inet"
          # pets:
          #   type: $pets
        # entries
        implements:
          - person
        indexes:
          blorpy:
            keys:
              - column: "lastname"
            type: "primary"
      pets:
        columns:
          name:
            type: "varchar"
            length: 64
            nullable: False
          species:
            type: "varchar"
            length: 64
            nullable: False
          # TODO: Check that FK type matches reference type.
          ownerFirstName:
            type: "varchar"
            length: 64
            nullable: True
          ownerLastName:
            type: "varchar"
            length: 64
            nullable: True
        foreignkeys:
          owner:
            columns:
              - ownerFirstName
              - ownerLastName
            referenceTable: people
            referenceColumns:
              - firstName
              - lastName

    # views:
    # functions: